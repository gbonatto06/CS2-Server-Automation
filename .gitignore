# Terraform State / Estado do Terraform
# Contains sensitive infrastructure data and should never be committed.
# Contém dados sensíveis da infraestrutura e nunca deve ser enviado.
*.tfstate
*.tfstate.*

# Variables & Secrets / Variáveis e Segredos
# Ignore actual credentials and local environment configs.
# Ignora credenciais reais e configurações de ambiente locais.
*.tfvars
*.tfvars.json
.env

# Initialization / Inicialização
# Ignore binary providers and local modules downloaded during 'terraform init'.
# Ignora binários dos providers e módulos locais baixados durante o 'init'.
.terraform/

# SSH Keys / Chaves SSH
# Private keys generated by the project (security risk).
# Chaves privadas geradas pelo projeto (risco de segurança).
*.pem

# System & Logs / Sistema e Logs
# Temporary logs and lock files.
# Logs temporários e arquivos de trava.
*.log
.terraform.lock.hcl

# Build Artifacts / Artefatos de Build
# ZIP files used for script deployment.
# Arquivos ZIP usados para o deploy dos scripts.
*.zip