apiVersion: 1
datasources:
  - name: Loki
    type: loki
    uid: Loki # Explicit UIDs to ensure linking with the Dashboard
    access: proxy
    url: http://localhost:3100
    isDefault: false
  - name: Prometheus
    type: prometheus
    uid: Prometheus # Explicit UIDs to ensure linking with the Dashboard
    access: proxy
    url: http://localhost:9090
    isDefault: true